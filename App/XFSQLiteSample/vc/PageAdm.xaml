<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XFSQLiteSample.vc.PageAdm">
    <ContentPage.Content>
        <StackLayout  BackgroundColor="#3EB489">
            
            <Label Margin="0,30,0,0" HorizontalOptions="CenterAndExpand" Text="Aqui tem todos os dados de todos os utilizadores disponiveis"></Label>
            <Entry TextColor="White" x:Name="nomeUSR" Placeholder="Nome de Utilizador" Margin="0,0,0,0"></Entry>

            <CollectionView x:Name="collectionView" SelectionMode="Single" SelectionChanged="collectionView_SelectionChanged" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Label TextColor="White" Text="Nome de utilizador" FontSize="Large" />
                            <Label TextColor="White" Text="{Binding Name}" FontSize="Medium" />
                            <Label TextColor="White" Text="Quantidade de cozidos" FontSize="Large" />
                            <Label TextColor="White" Text="{Binding QuantCoz}" FontSize="Medium" />
                            <Label TextColor="White" Text="Tipo de cozido" FontSize="Large" />
                            <Label TextColor="White" Text="{Binding TipoCoz}" FontSize="Medium" />
                            <Label TextColor="White" Text="--Próximo utilizador-" FontSize="Large" />

                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button Margin="0,0,0,0" Text="Atualizar utilizador" Clicked="Button_Clicked_1"></Button>
            <Button Margin="0,0,0,0" Text="Apagar utlizador" Clicked="Button_Clicked_2"></Button>
            <Button Margin="0,0,0,0" Text="Quem tem um cozinhado a cozer" Clicked="Button_Clicked_3"></Button>
            <Button Margin="0,0,0,0" Text="Quem não tem um cozinhado a cozer" Clicked="Button_Clicked_4"></Button>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>