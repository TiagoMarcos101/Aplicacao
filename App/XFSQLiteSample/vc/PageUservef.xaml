<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XFSQLiteSample.vc.PageUservef">
    <ContentPage.Content>
        <StackLayout BackgroundColor="#b7ffff">
            <Entry  PlaceholderColor="Black" TextColor="Black" x:Name="nameUSR" Margin="0,30,0,0" Placeholder="Coloque aqui o seu nome de utilizador"></Entry>
            <Button Background="white" TextColor="Black" Text="Verifique seus dados" Clicked="Button_Clicked"></Button>
            <StackLayout>

                <CollectionView x:Name="collectionView" >
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout>
                                <Label TextColor="Black" Text="Nome de utilizador"></Label>
                                <Label TextColor="Black" Text="{Binding Name}" FontSize="Medium" />
                                <Label TextColor="Black" Text="Quantidade de cozidos"></Label>
                                <Label TextColor="Black" Text="{Binding QuantCoz}" FontSize="Medium" />
                                <Label TextColor="Black" Text="Tipo de cozidos sendo 0 cozido e 1 caldeirada"></Label>
                                <Label TextColor="Black" Text="{Binding TipoCoz}" FontSize="Medium" />
                                <Label TextColor="Black" Text="Hora que o cozido foi inserido"></Label>
                                <Label TextColor="Black" Text="{Binding Time}" FontSize="Medium" />
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                <Image  Margin="0,0,0,0" Source="https://i.ytimg.com/vi/K85G4iSF19Q/maxresdefault.jpg"></Image>

            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>